# Package does not load .env automatically; export values through your shell.
# Required only when tracking.mode=online.
WANDB_API_KEY=

# Required for experiment=transunet. Pass them to the Hydra third_party.* keys.
TRANSUNET_ROOT=
TRANSUNET_PRETRAINED=

# Notebook-only TOKEN_MIXER_* variables are documented in README.md and are
# intentionally omitted from this package/runtime template.
